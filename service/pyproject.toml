[project]
name = "boardgame-rules-service"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = [
    "boardgame-rules-backend",
    "boardgame-rules-bot",
]

[dependency-groups]
dev = [
    "pre-commit>=4.5.1",
]
